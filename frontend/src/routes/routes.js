const publicRoutes = {
  home: '/',
  collection: '/collection',
  cart: '/cart',
  search: '/search',
  notFound: '*',
};

const privateRoutes = {
  login: '/login',
};

export { publicRoutes, privateRoutes };
